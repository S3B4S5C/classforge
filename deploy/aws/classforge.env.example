# Copy to /etc/classforge/classforge.env, chmod 600, and replace every CHANGE_ME.
AWS_REGION=us-east-1

# Keep model selection outside the binary. Nova Premier reached EOL on 2026-09-14;
# Nova 2 Lite is the conservative active default for the deployment profile.
CLASSFORGE_BEDROCK_TEXT_MODEL=us.amazon.nova-2-lite-v1:0
CLASSFORGE_BEDROCK_VISION_MODEL=us.amazon.nova-2-lite-v1:0
CLASSFORGE_BEDROCK_TIMEOUT_SECONDS=300
CLASSFORGE_BEDROCK_TEXT_MAX_TOKENS=1024

# The EC2 IAM role supplies AWS credentials. Never put AWS_ACCESS_KEY_ID here.
CLASSFORGE_JWT_SECRET=CHANGE_ME_LONG_RANDOM_SECRET_AT_LEAST_48_CHARS

# After CloudFront exists, replace this with its exact origin, for example:
# https://d1234567890abc.cloudfront.net
CLASSFORGE_WEBSOCKET_ALLOWED_ORIGINS=https://*.cloudfront.net

# whisper.cpp remains private on the same EC2 instance.
CLASSFORGE_ASSISTANT_WHISPER_URL=http://127.0.0.1:8093

# Vision pipeline remains configurable without changing code.
CLASSFORGE_ASSISTANT_VISION_TWO_PASS=false
CLASSFORGE_ASSISTANT_VISION_HYBRID=true
CLASSFORGE_ASSISTANT_VISION_HYBRID_MIN_CLASSES=4
CLASSFORGE_ASSISTANT_VISION_HYBRID_FALLBACK=false
